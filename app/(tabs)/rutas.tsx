import { View, Text } from "react-native";

export default function RutasScreen() {
    return (
        <View className="flex-1 items-center justify-center bg-background">
            <Text className="text-text text-xl mb-6">Rutas</Text>
        </View>
    );
}